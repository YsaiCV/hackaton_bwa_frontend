void saveFile(List<int> bytes, String fileName) {
  throw UnsupportedError('File download is only supported on Web.');
}

void openInNewTab(String url) {
  throw UnsupportedError('Opening links in a new tab is only supported on Web.');
}
